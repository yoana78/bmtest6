name = "boomyung-site"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

[[d1_databases]]
binding = "DB"
database_name = "boomyung-sitef-db"
database_id = "a9839197-c41e-4d32-926a-e9aaba1ed47b"

[ai]
binding = "AI"

# ADMIN_PASSWORD is set as a secret (not in this file):
#   npx wrangler pages secret put ADMIN_PASSWORD --project-name=bmtest6
#
# If the D1 binding above isn't picked up automatically by a git-triggered
# Pages build, add it manually once in the dashboard:
# Workers & Pages -> bmtest6 -> Settings -> Functions -> D1 database bindings
# -> variable name "DB" bound to "boomyung-sitef-db".
